
import sys, getopt

def main(argv):
    # TODO: add a way to parse argument, search google for:
    #  opts, args = getopt.getopt(argv, "hi:o:", ["ifile=", "ofile="])

    # init log file
    # set seeds (for random functions)
    # device: cuda, cpu
    # create the network
    # if task == "train"/"test"
    #   init "test"/"train"
    #   run "test"/"train"
    # result + plots
    pass


if __name__ == "__main__":
    main(sys.argv[1:])
